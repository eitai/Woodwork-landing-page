# IDB — אתר תדמית עם scroll-scrub של פרגולה

עסק: **IDB** — בניית פרגולות ודקים.
תפיסה: ככל שגוללים, פרגולה מתפרקת לחלקים שצפים באוויר ואז נבנית מחדש — עד סצנה מרוהטת וחיה.
בסיס טכני: אותו stack של אתר תדמית / אתר המוזיקה — **Vite + React + TypeScript + GSAP ScrollTrigger + canvas scroll-scrub**. עברית RTL.

---

## 1. הרעיון (thesis)

ה-Hero הוא ההוכחה לעצמו: לקוח רואה את המוצר של IDB **נבנה מול העיניים שלו**. זה בדיוק מה שהעסק מוכר — הרכבה מדויקת של מבנה עץ. ה-scrub הוא לא גימיק, הוא ההצהרה.

הלופ בקצוות (וידאו של אנשים יושבים בפרגולה בערב) נותן את ה"אחרי" — למה שווה לבנות.

---

## 2. אסטים (כבר הופקו ב-Higgsfield)

| קוד | תיאור | Higgsfield job id |
|---|---|---|
| **A** | פרגולה מרוהטת מלאה (anchor) | `5a7dc33a-4f4f-4a46-9ed7-4689e267c58f` |
| **B** | סטיל אנשים יושבים (בסיס ללופ) | `bc7bd983-af0c-4dfb-add5-ddca9d41cea2` |
| **C** | פרגולה+דק ריקים (מבנה שלם, בלי ריהוט) | `13db9a1c-b42f-4d55-91cf-51031746b203` |
| **D** | חלקים צפים באוויר (מפורק) | `aedfba38-048e-436e-903e-edbc3492bfc6` |

### קליפים (3) — להוריד מ-Higgsfield ל-`clips/` עם prefix מספרי:

| קובץ | תוכן | job id | שימוש |
|---|---|---|---|
| `0_loop.mp4` | אנשים בפרגולה, תנועה עדינה (loop) | `873e4672-39a8-40c9-94b1-25659170fbdd` | **וידאו bookend** — לא עובר scrub |
| `1_assembly.mp4` | D→C: החלקים הצפים מתחברים למבנה | `64ae3ee3-47e1-44de-b92c-072000a7b01d` | scrub (הפוך=פיצוץ, ישר=הרכבה) |
| `2_furnish.mp4` | C→A: הריהוט/מנורות/עציצים מגיעים | `be078c28-fd47-4952-af88-f407e1960145` | scrub |

> גיבוי (אם קליפ ההרכבה יתגלה כלא חלק): stills ביניים 25/50/75 —
> `239757ba…`, `c292ab34…`, `b3cb6705…`. אפשר להזין אותם כ-keyframes ל-`assemble` במקום frames מהקליפ.

---

## 3. טיימליין ה-Hero (single pinned stage)

**מנוחה (לפני גלילה):** `0_loop.mp4` מתנגן autoplay muted loop — אנשים בפרגולה. Overlay: לוגו IDB + טאגליין + חץ "גלול".

**בתוך ה-pinned stage (scrub):** במקום להתנגן — נשלט לפי גלילה. סגמנט אחד = canvas layer.

| progress | סגמנט | מיפוי frame | מה רואים |
|---|---|---|---|
| 0.00–0.06 | dissolve | — | הלופ (מרוהט) → C (ריק). ריהוט נעלם. |
| 0.06–0.30 | `assemble` **הפוך** | frame: last→first | הפרגולה **מתפוצצת** (C→D) |
| 0.30–0.72 | `assemble` **ישר** | frame: first→last | הפרגולה **נבנית** (D→C) |
| 0.72–0.94 | `furnish` ישר | frame: first→last | ריהוט/מנורות/עציצים **מגיעים** (C→A) |
| 0.94–1.00 | dissolve | — | canvas(A) → `0_loop.mp4` חוזר |

**סוף ה-stage:** הלופ מתנגן שוב (אנשים) — סוגר את הלופ הרעיוני.

> הערה: סגמנט `assemble` משמש פעמיים (הפוך→ישר). זה ping-pong על אותם frames — לא צריך לחלץ פעמיים. פונקציית `progress→frameIndex` מטפלת בזה (ראה §5).

---

## 4. ארכיטקטורה (מהסקיל scroll-scrub)

- **stage אחד** שעושה pin פעם אחת. אורך ה-pin = סכום מרחקי ה-scrub של הסגמנטים.
- כל סגמנט = layer (`position:absolute; inset:0`, canvas אחד).
- מיפוי progress יחיד בוחר סגמנט פעיל + progress מקומי, ועושה **cross-dissolve** באוברלאפ ~6%.
- אף פעם לא שני סגמנטים גלויים בו-זמנית. אין תפר.
- `scrub: 1.5` (מספרי) — תחושה משוקללת.
- מרחק scroll: תן ל-`assemble` את הכי הרבה (זו ההצגה), ל-`furnish` פחות.

**וידאו הלופ** הוא `<video>` נפרד (לא scrub) שגלוי במנוחה ובסוף ה-stage, ומתחלף ל/מ-canvas ב-dissolve. זה החריג היחיד ל"אין וידאו ב-runtime" — כי זה ambient bookend, לא ה-scrub עצמו.

---

## 5. מיפוי frame ל-`assemble` (ping-pong)

```
localToFrame(seg, p):   // p = progress גלובלי של ה-stage
  count = manifest.count
  if seg == "assemble":
     if p < 0.30:        // פיצוץ: C→D  (הפוך)
        t = (p - 0.06) / (0.30 - 0.06)   // 0..1
        idx = round((1 - t) * (count-1))
     else:               // הרכבה: D→C (ישר)
        t = (p - 0.30) / (0.72 - 0.30)   // 0..1
        idx = round(t * (count-1))
  if seg == "furnish":
     t = (p - 0.72) / (0.94 - 0.72)
     idx = round(t * (count-1))
  return clamp(idx, 0, count-1)
```

opacity:
- הלופ-וידאו: גלוי p<0.06 ו-p>0.94 (dissolve בקצוות).
- `assemble`: גלוי 0.04–0.74.
- `furnish`: גלוי 0.70–0.96.

---

## 6. מבנה האתר

```
[Hero — pinned scrub]        ← הפרגולה נבנית
[מי אנחנו]                    ← מי זה IDB, למה לבחור בהם
[עבודות שלנו]                 ← גלריה (placeholder → תמונות אמת בהמשך)
[צור קשר]                     ← וואטסאפ + מייל
[footer]
```

### תוכן (עברית, RTL)

**מי אנחנו** — כותרת: "בונים לכם את החוץ." גוף: IDB מתמחים בפרגולות ודקים בהתאמה אישית — תכנון, חומרים ועבודת נגרות מדויקת, מהשרטוט ועד המבנה המוגמר. דגש: אחריות, גימור, עמידות בשמש/גשם. (Claude Code — נסח 2–3 פסקאות + 3 "יתרונות" קצרים בסגנון spec: תכנון בהתאמה • חומרים איכותיים • התקנה מקצועית.)

**עבודות שלנו** — גריד 6–9 כרטיסים בגריד wood-framed. עכשיו placeholder (השתמש ב-A ובסטילים הריקים/מרוהטים כזמניים). כשיגיעו תמונות אמת — החלף. כל כרטיס: תמונה + שורת כיתוב קצרה (סוג פרויקט / מיקום).

**צור קשר** — כותרת: "רוצים פרגולה כזאת?" שני CTA:
- **וואטסאפ** (כפתור ראשי): `https://wa.me/972XXXXXXXXX?text=...` (מלא מספר).
- **מייל**: `mailto:info@idb.co.il` (מלא כתובת).
אין טופס, אין טלפון — רק וואטסאפ + מייל (לפי בקשת הלקוח).

---

## 7. שפה עיצובית (design tokens)

הבסיס: עולם הנגרות + golden-hour. לא cream-serif-terracotta גנרי — עץ אמיתי, אור חם, דיוק אדריכלי.

**צבעים:**
```
--espresso:  #2A1E15   /* עץ כהה, רקע hero / טקסט */
--cedar:     #B5763F   /* עץ ארז — accent ראשי */
--amber:     #E3A85C   /* אור golden-hour — glow / hover */
--bone:      #F2EADD   /* קרם חמים — רקע בהיר */
--olive:     #6E7355   /* ירוק גינה — secondary */
--ink:       #1C1712   /* כמעט-שחור חמים */
```

**טיפוגרפיה (עברית):**
- Display: **Frank Ruhl Libre** (סריף עברי פרימיום, חם) — כותרות, בשימוש מדוד.
- Body: **Assistant** (סן-סריף עברי נקי) — טקסט.
- Utility/מספרים: **IBM Plex Mono** — למידות/spec (למשל `6.0m × 4.0m`, `40mm ארז`). זה ה-flavor האדריכלי.

**Signature:** קווי מידה דקים + מספרים ב-mono על הפרגולה במנוחה (כמו שרטוט אדריכל) — עדין, מופיע/נעלם. מחזק "הנדסה מדויקת". אל תגזים: אלמנט אחד בולט (ה-scrub), השאר שקט ומדויק.

**RTL:** `dir="rtl"`, לוגו בצד ימין, ניווט מימין לשמאל.

---

## 8. SEO / ביצועים
- prerender (vite-plugin-prerender), כותרות אמת crawlable, schema `LocalBusiness` + `Service`.
- mobile: החלף frame-scrub ב-poster/וידאו לופ קצר; הורד frame count.
- `prefers-reduced-motion`: החזק static, השאר את הלופ רגוע.
- WebP frames, decode ב-`createImageBitmap`, evict סגמנטים רחוקים.

---

## 9. סדר בנייה (de-risk — מהסקיל)
1. הורד 3 קליפים ל-`clips/` בשמות `0_loop`,`1_assembly`,`2_furnish`. הרץ `build-frames.sh`. ודא frames+manifest בכל תיקייה.
2. בנה סגמנט אחד (`assemble`) end-to-end ב-stage היחיד — הוכח שה-pin נאחז, scrub לשני הכיוונים, poster מיידי.
3. הוסף `furnish` כ-layer עם cross-dissolve.
4. חבר את וידאו הלופ בקצוות (dissolve in/out).
5. סקשנים (מי אנחנו / עבודות / צור קשר).
6. perf + mobile + reduced-motion אחרון.
